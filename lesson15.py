# constants are written in CAPITAL CASE to identify them (it's a convention)
# don't write complex if
# keep the code simple


# the id of same value will be the same:

var1 = 'a'
var2 = 'a'

print(id(var1)) # 4375552368
print(id(var2)) # 4375552368